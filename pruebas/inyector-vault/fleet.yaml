defaultNamespace: vault
helm:
  chart: vault
  repo: https://helm.releases.hashicorp.com
  version: 0.30.0
  releaseName: vault
  values:
    injector:
      enabled: true
      env:
        AGENT_INJECT_VAULT_ADDR: "http://10.188.106.55:8200"
        VAULT_ADDR: "http://10.188.106.55:8200"
    server:
      enabled: false
    externalVault:
      addr: http://10.188.106.55:8200
      skipVerify: true
